NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
APP_ORIGIN=http://localhost:3000
# Local workers and /api/trial server only. Never expose this key in a browser.
SUPABASE_SECRET_KEY=
# Cursor credentials and CLI paths stay on the worker PC, not the web deployment.
CURSOR_CLI_PATH=
CURSOR_NODE_PATH=
CURSOR_WORKSPACE=
